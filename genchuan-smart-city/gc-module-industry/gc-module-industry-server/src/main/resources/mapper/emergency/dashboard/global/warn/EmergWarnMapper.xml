<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.industry.dal.mysql.emergency.dashboard.global.warn.EmergWarnMapper">

    <!-- 查询近期应急预警（例如：最新 10 条，可根据需要调整） -->
    <select id="getEmergWarn"
            resultType="cn.iocoder.yudao.module.industry.controller.admin.emergency.dashboard.global.warn.vo.EmergWarnRespVO">
        SELECT
            type,
            discovery_time AS discoveryTime,
            status,
            description,
            region_name AS regionName
        FROM biz_emerg_warn
        ORDER BY discovery_time DESC
        LIMIT 10
    </select>

</mapper>
